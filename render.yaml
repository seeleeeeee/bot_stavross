services:
  - type: web
    name: bot-stavross
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python Bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false